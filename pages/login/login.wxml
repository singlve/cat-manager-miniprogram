<!-- pages/login/login.wxml -->
<view class="login-page {{themeClass}}">
  <view class="login-shell">

    <!-- Logo -->
    <view class="login-brand">
      <include src="../../templates/festival-hero-decor.wxml" />
      <image class="login-hero-img" src="/assets/illustrations/home-hero.webp" mode="aspectFit" />
      <text class="brand-kicker">PET CARE</text>
      <text class="brand-title">宠物小管家Plus</text>
      <text class="brand-subtitle">记录宠物成长，守护每一刻</text>
    </view>

    <!-- 微信一键登录 -->
    <button class="wx-login-btn" open-type="getPhoneNumber" bindgetphonenumber="onWxLogin">
      <view class="wx-login-content">
        <text>微信一键登录</text>
      </view>
    </button>

    <!-- 其他方式 -->
    <view class="other-login-toggle" bindtap="toggleOtherLogin">
      <text>其他方式登录</text>
      <view class="toggle-arrow {{showOtherLogin ? 'is-open' : ''}}">
        <view class="toggle-chevron"></view>
      </view>
    </view>

    <!-- 手机号密码登录（折叠区） -->
    <view wx:if="{{showOtherLogin}}" class="phone-login-card">
      <view class="phone-login-head">
        <text class="phone-login-title">手机号登录</text>
        <text class="phone-login-desc">使用已注册手机号和密码进入</text>
      </view>
      <view class="login-field">
        <text class="login-field-label">手机号</text>
        <input class="login-input" type="number" placeholder="请输入手机号" bindinput="phoneInput" maxlength="11" placeholder-style="color:#A5AFBC;" />
      </view>
      <view class="login-field">
        <text class="login-field-label">密码</text>
        <input class="login-input" type="password" placeholder="请输入密码" bindinput="passwordInput" placeholder-style="color:#A5AFBC;" />
      </view>
      <view class="phone-login-btn" bindtap="login">手机号登录</view>
      <view class="register-row">
        <text>还没有账号？</text>
        <view bindtap="goRegister"><text class="register-link">立即注册</text></view>
      </view>
    </view>

  </view>
</view>

<!-- 完善资料弹窗（新用户绑定手机后弹出） -->
<view wx:if="{{showProfileSetup}}" class="profile-setup-mask">
  <view class="profile-setup-panel" catchtap="stopBubble">
    <view class="profile-setup-accent"></view>
    <text class="profile-setup-kicker">WELCOME</text>
    <text class="profile-setup-title">完善资料</text>
    <text class="profile-setup-desc">设置头像和昵称，让照护记录更有归属感。</text>

    <!-- 头像选择 -->
    <view class="profile-avatar-section">
      <view class="profile-avatar-wrap">
        <view class="avatar-ring"></view>
        <button class="avatar-choose-btn" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
          <image src="{{profileAvatar || '/assets/illustrations/profile-mascot.webp'}}" mode="aspectFill" class="avatar-preview" />
        </button>
      </view>
      <text class="profile-avatar-hint">点击选择头像</text>
    </view>

    <!-- 昵称输入 -->
    <view class="profile-nickname-field">
      <input type="nickname" placeholder="请输入昵称" bindinput="profileNicknameInput" value="{{profileNickname}}"
        class="profile-nickname-input" placeholder-style="color:#A5AFBC;" />
    </view>

    <view class="profile-setup-confirm" bindtap="saveProfile">完成</view>
    <view class="profile-setup-skip" bindtap="skipProfileSetup">
      <text>稍后再说</text>
      <view class="profile-skip-chevron"></view>
    </view>
  </view>
</view>
